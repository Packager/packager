// import { generateSourceMap } from "../";

describe("generate source map", () => {
    it("should return true", () => expect(true).toBeTruthy());
    //     it("should generate a source map correctly", () => {
    //         const filePath = "/src/app.js";
    //         const originalCode = `export default {
    //     name: 'packager'
    // }`;
    //         const generateCode = `var app = {
    //     name: 'packager'
    // }`;
    //         expect(generateSourceMap(filePath, originalCode, generateCode)).toBe(
    //             ""
    //         );
    //     });
});
